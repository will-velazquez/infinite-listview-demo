<Page
    x:Class="Fantastical.App.Pages.ItemsRepeaterBugTest"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fantastical.App.Pages">
    <FrameworkElement.Resources>
        <DataTemplate x:Key="TheTemplate" x:DataType="local:Person">
            <TextBlock Text="{x:Bind Name}" />
        </DataTemplate>
    </FrameworkElement.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Button
            Content="Foo"
            Click="Button_Click" />

        <ItemsRepeater
            Grid.Row="1"
            ItemsSource="{x:Bind People}">
            <ItemsRepeater.ItemTemplate>
                <DataTemplate x:DataType="local:Person">
                    <ContentPresenter 
                        Content="{x:Bind}"
                        ContentTemplate="{StaticResource TheTemplate}" />
                </DataTemplate>
            </ItemsRepeater.ItemTemplate>
        </ItemsRepeater>
    </Grid>
</Page>
